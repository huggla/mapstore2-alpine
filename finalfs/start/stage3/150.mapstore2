# Set in stage2:
# ---------------------------------------------------------
# set -ef +am
# isFirstRun
# VAR_*
# All functions in /start/functions

if [ "$isFirstRun" == "true" ]
then
   replaceFileWithLink "$VAR_WEBAPPS_DIR/ROOT/localConfig.json" "$VAR_CONFIG_DIR/localConfig.json"
   replaceFileWithLink "$VAR_WEBAPPS_DIR/ROOT/WEB-INF/classes/users.ldif" "$VAR_CONFIG_DIR/users.ldif"
fi
