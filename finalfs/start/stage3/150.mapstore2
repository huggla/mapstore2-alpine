# Set in stage2:
# ---------------------------------------------------------
# set -ef +am
# isFirstRun
# VAR_*
# All functions in /start/functions

configDirContent="$(ls -A "$VAR_CONFIG_DIR")"
if [ ! -z "$configDirContent" ] && [ "$configDirContent" != ".snapshot" ]
then
   cp -a $VAR_CONFIG_DIR/* "$VAR_WEBAPPS_DIR/ROOT/WEB-INF/lib/"
fi
